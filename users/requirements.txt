sqlalchemy==1.4.11
asyncpg==0.27.0  # 0.22 has no aarch64 wheel
pyhumps==1.6.1
alembic==1.5.8
pydantic[email]==1.10.13  # 1.8.1 crashes on Literal under py3.8
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 breaks on bcrypt >= 4.1
python-jose==3.2.0
python-multipart==0.0.5
fastapi==0.63.0
uvicorn[standard]==0.17.6  # was supplied by the archived base image

arq==0.20.0
# arq 0.20 declares `aioredis>=1.1.0` with no upper bound, but imports
# `MultiExecError`, which aioredis 2.0 removed. Unpinning breaks the API and the
# worker at import. Delete this when arq >= 0.23 moves us to redis-py.
aioredis==1.3.1
uvloop==0.15.2
