# Trimmed from users/requirements.txt. Dropped because this service has no
# accounts and, until stage 2, no jobs: bcrypt, pyjwt, python-multipart (login
# form parsing), arq and uvloop. pyhumps went with the tablename convention,
# and pydantic needs no [email] extra without EmailStr.
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.19.1
pydantic==2.13.5
pydantic-settings==2.15.0
fastapi==0.141.1
uvicorn[standard]==0.52.4
# Back for stage 4: canvas verifies tokens users signed. [crypto] is what makes
# RS256 available; the private half never comes near this service.
pyjwt[crypto]==2.13.0
